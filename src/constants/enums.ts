export enum APPROVAL_STATUS {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}

export enum APPROVAL_ACTION {
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
}
